#!/usr/bin/env bash
#set -x
# Purpose: Start bspwm session daemons exactly once and clean up stale PID-managed leftovers.
# Notes:
#   - Uses XDG_RUNTIME_DIR for lock and PID files.
#   - Cleans up known stale PID files from supervised components.
#   - Starts each component only if not already running.
#   - Works across repeated bspwm restarts (bspc wm -r).

set -o errexit
set -o nounset
set -o pipefail

###############################################################################
# Environment and paths
